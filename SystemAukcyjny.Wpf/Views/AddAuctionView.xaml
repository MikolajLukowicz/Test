<UserControl x:Class="SystemAukcyjny.Wpf.Views.AddAuctionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="400">
    <Grid Margin="20">
        <StackPanel>
            <TextBlock Text="Dodaj Nową Aukcję" FontSize="20" FontWeight="Bold" Margin="0,0,0,20"/>

            <TextBlock Text="Tytuł:"/>
            <TextBox Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>

            <TextBlock Text="Opis:"/>
            <TextBox Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}" Height="100" TextWrapping="Wrap" Margin="0,0,0,10"/>

            <TextBlock Text="Kategoria:"/>
            <ComboBox ItemsSource="{Binding Categories}" SelectedItem="{Binding SelectedCategory}" DisplayMemberPath="NazwaKategorii" Margin="0,0,0,10"/>

            <TextBlock Text="Cena wywoławcza:"/>
            <TextBox Text="{Binding StartingPrice, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>

            <TextBlock Text="Data zakończenia:"/>
            <DatePicker SelectedDate="{Binding EndDate}" Margin="0,0,0,10"/>

            <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" TextWrapping="Wrap" Margin="0,0,0,10"/>

            <Button Content="Wystaw Aukcję" Command="{Binding AddAuctionCommand}" Padding="10,5"/>
        </StackPanel>
    </Grid>
</UserControl>
