<UserControl x:Class="SystemAukcyjny.Wpf.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="400">
    <Grid VerticalAlignment="Center" HorizontalAlignment="Center">
        <Border BorderBrush="#FF5A00" BorderThickness="2" Padding="30" CornerRadius="10" Background="White">
            <Border.Effect>
                <DropShadowEffect BlurRadius="15" Direction="270" RenderingBias="Quality" ShadowDepth="1" Color="Gray"/>
            </Border.Effect>
            <StackPanel Width="250">
                <TextBlock Text="LOGOWANIE" FontSize="26" FontWeight="ExtraBold" Foreground="#FF5A00" HorizontalAlignment="Center" Margin="0,0,0,25"/>

                <TextBlock Text="Login:" FontWeight="SemiBold" Margin="0,0,0,5"/>
                <TextBox Text="{Binding UserLogin, UpdateSourceTrigger=PropertyChanged}" Padding="5" Margin="0,0,0,15"/>

                <TextBlock Text="Hasło:" FontWeight="SemiBold" Margin="0,0,0,5"/>
                <PasswordBox x:Name="PasswordBox" Padding="5" Margin="0,0,0,20"/>

                <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" FontSize="11" Margin="0,0,0,15" TextWrapping="Wrap" HorizontalAlignment="Center"/>

                <Button Content="Zaloguj" Command="{Binding AuthenticateCommand}" Padding="10,8" Background="#FF5A00" Foreground="White" FontWeight="Bold" Cursor="Hand">
                </Button>

                <Button Content="Nie masz konta? Zarejestruj się" Command="{Binding GoToRegisterCommand}" Background="Transparent" BorderThickness="0" Foreground="#FF5A00" Margin="0,15,0,0" Cursor="Hand" TextElement.FontSize="12"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
