<Window x:Class="SystemAukcyjny.Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SystemAukcyjny.Wpf"
        xmlns:vm="clr-namespace:SystemAukcyjny.Wpf.ViewModels"
        xmlns:v="clr-namespace:SystemAukcyjny.Wpf.Views"
        mc:Ignorable="d"
        Title="allegro 2.0" Height="700" Width="1100"
        Background="#F5F5F5">
    <Window.Resources>
        <DataTemplate DataType="{x:Type vm:AuctionListViewModel}">
            <v:AuctionListView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:MyAuctionsViewModel}">
            <v:MyAuctionsView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:AddAuctionViewModel}">
            <v:AddAuctionView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:InvestmentBotViewModel}">
            <v:InvestmentBotView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:LoginViewModel}">
            <v:LoginView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:RegisterViewModel}">
            <v:RegisterView/>
        </DataTemplate>

        <local:NullToVisibilityConverter x:Key="NullToVisibilityConverter"/>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Sidebar - Only visible when logged in -->
        <Border Grid.Column="0" Background="#FF5A00" Width="220"
                Visibility="{Binding IsUserLoggedIn, Converter={StaticResource BooleanToVisibilityConverter}}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Margin="0,30,0,40">
                    <TextBlock Text="allegro" Foreground="White" FontSize="32" FontWeight="Bold" HorizontalAlignment="Center"/>
                    <TextBlock Text="wersja 2.0" Foreground="White" FontSize="14" HorizontalAlignment="Center" Opacity="0.8"/>
                </StackPanel>

                <StackPanel Grid.Row="1" Margin="10,0">
                    <Button Content="🔍 Aukcje" Command="{Binding GoToAuctionsCommand}" Margin="0,5" Padding="15,10" Background="Transparent" Foreground="White" BorderThickness="0" HorizontalContentAlignment="Left" FontWeight="SemiBold"/>
                    <Button Content="👤 Moje Aukcje" Command="{Binding GoToMyAuctionsCommand}" Margin="0,5" Padding="15,10" Background="Transparent" Foreground="White" BorderThickness="0" HorizontalContentAlignment="Left" FontWeight="SemiBold"/>
                    <Button Content="➕ Dodaj Aukcję" Command="{Binding GoToAddAuctionCommand}" Margin="0,5" Padding="15,10" Background="Transparent" Foreground="White" BorderThickness="0" HorizontalContentAlignment="Left" FontWeight="SemiBold"/>
                    <Button Content="🤖 Bot Inwestycyjny" Command="{Binding GoToInvestmentBotCommand}" Margin="0,5" Padding="15,10" Background="Transparent" Foreground="White" BorderThickness="0" HorizontalContentAlignment="Left" FontWeight="SemiBold"/>
                </StackPanel>

                <Button Grid.Row="2" Content="🚪 Wyloguj" Command="{Binding LogoutCommand}"
                        Margin="15,0,15,20" Padding="10,8" Background="#333333" Foreground="White" BorderThickness="0" FontWeight="Bold"/>
            </Grid>
        </Border>

        <!-- Content Area -->
        <Grid Grid.Column="1">
            <ContentControl Content="{Binding CurrentContentViewModel}"/>
        </Grid>
    </Grid>
</Window>
